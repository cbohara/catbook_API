version: '2'
services:
  app:
    build: .
    links:
      - db
    ports:
      - "3001:3000"
    volumes:
      - .:/usr/src/app
    volumes_from:
      - gems
  db:
    image: postgres:9
  gems:
    image: busybox
    volumes:
      - /ruby_gems
